# Streamlit Cloud Secrets Configuration
# Copy this format when adding secrets in Streamlit Cloud

# Google API Key
GOOGLE_API_KEY = "your_google_api_key_here"

# Note: In Streamlit Cloud, you enter secrets in the UI
# The format should be:
# GOOGLE_API_KEY=your_actual_key_here
# (without quotes in the UI, but TOML format requires quotes)

